project_id        = "${PROJECT_ID}"
region            = "${REGION}"
tf_state_bucket   = "${TF_STATE_BUCKET}"
emqx_domain       = "${DOMAIN}"
iap_support_email = "${IAP_SUPPORT_EMAIL}"
emqx_broker_type  = "${EMQX_BROKER_TYPE}"
